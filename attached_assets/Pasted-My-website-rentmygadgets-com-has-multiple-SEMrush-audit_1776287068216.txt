My website rentmygadgets.com has multiple SEMrush audit errors and warnings that all need to be fixed in one go. Please fix everything listed below completely.
For canonicalization and duplicate content, set up a permanent 301 redirect so all traffic coming to https://www.rentmygadgets.com automatically redirects to https://rentmygadgets.com making the non-www version the only canonical version of the site, and add a canonical tag in the head section of every page pointing to the correct non-www URL.
For meta tags, give every page its own unique and descriptive title tag containing relevant keywords specific to that page so no two pages share the same title, and write a unique meta description for every page between 120 and 160 characters describing only that page's content so no two pages share the same meta description.
For H1 tags, add a clear and relevant H1 heading on every page that describes the main topic of that page, for example the homepage should have an H1 like Rent Premium Tech Equipment in India including laptops cameras and more.
For page content, add at least 300 meaningful words of visible readable text on each page covering what gadgets are available for rent, how the rental process works, why customers should choose rentmygadgets, service areas, pricing overview, and any other relevant business information so SEMrush no longer flags the page for little content.
For text to HTML ratio, move all inline CSS styles into a separate external stylesheet file and move all inline and embedded JavaScript into separate external JS files so the HTML becomes lighter and the ratio of visible text to code improves well above 10 percent.
For internal linking, update the navigation menu to include links to all main pages of the site, add a footer section with links to all important pages, and add contextual internal links within page content wherever relevant so every page has multiple incoming internal links from other pages.
For compression, enable Gzip or Brotli compression on the server so all HTML pages and assets are served compressed, and minify all JavaScript and CSS files to reduce their file size.
For robots.txt and sitemap, make sure the robots.txt file in the root allows all bots with no disallow rules and references the sitemap using the non-www https URL, and generate a complete sitemap.xml file listing all pages with loc, lastmod, changefreq, and priority tags placed at the root so it is accessible at https://rentmygadgets.com/sitemap.xml.
After completing all of these fixes tell me every single file that was modified and exactly what was changed in each file.